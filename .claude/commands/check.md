# Build Check

Run lint and build to verify the project compiles without errors.

## Instructions

1. Run `npm run lint` to check for ESLint issues
2. If lint passes, run `npm run build` to verify TypeScript compilation and Next.js build
3. Report any errors found with file locations and suggested fixes
4. If everything passes, confirm the project is ready for deployment
