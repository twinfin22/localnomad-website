{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "stage4-input.schema.json",
  "title": "Stage 4 Input",
  "description": "Combined input for Stage 4 Quality Gate: draft text plus Stage 2 and Stage 3 outputs.",
  "type": "object",
  "required": ["draftText", "stage2", "stage3"],
  "properties": {
    "draftText": {
      "type": "string",
      "description": "Full MDX draft content (including frontmatter) as a string."
    },
    "stage2": {
      "$ref": "stage2-output.schema.json",
      "description": "Stage 2 output: keywords, outline, internal link targets, etc."
    },
    "stage3": {
      "$ref": "stage3-output.schema.json",
      "description": "Stage 3 output: draft metadata, frontmatter, word count."
    }
  }
}
